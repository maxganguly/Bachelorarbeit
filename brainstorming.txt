Original
KorrekturTests
	testcases mit Punkten
	Semantic Similarity zu Musterlösung
		ast Structure comparison
		weakest precondition/strongest postcondition (Edge case detection)
	maybe
		testing if variables with correct value exist (JPDA debugger)
		code correction

Brainstorming
Give generated AST to modify to specify parts that are needed or forbidden
Side-effects (sysout) when calculating semantic similarity (post condition)
Remove not working methods from compilation 

Changes
Fully automatic -> Semi automatic
	A tool for the better generation of Testcases

Weakest precondition -> Strongest postcondition -> ?Symbolic Execution? -> MC/DC and Mutation testing 
	The precondition is always given and the postcondtition needs to match
	Maybe symbolic execution to generate edge cases
	As we have a "ground truth" we should be able to generate edge cases by using MC/DC and Mutation Testing (Fault Injection) on it to get Testcases
		for the fault injection, check if a injected fault results in a changed return/output if yes -> New testcase
		
Pseudosymbolic execution to get the MC/DC Testcases
	run through the Program each step and keep all variables in a map
		if a variable is given a new value inject the value into the variable
			func(int a)
			{a: a}
			int x = 12;
			{a: a,x: 12}
			int y = 3*a;
			{a: a,x: 12,y= 3*a}
			x += y;
			{a: a,x: 12+(3*a),y= 3*a}
			y -= x;
			{a: a,x: (12)+(3*a),y= (3*a)-(3*a)}
				Possible optimization
		on every condition save the condition and the negation with the current variables and branch to the next "line of code"
		maybe code unroll and saved in list
		branches müssen in die höhere Rekursionsebenen "zurückgegeben" werden
		List<String> params(AST, vars, branches(als List for pseudoreturn) List<Map_of_vars> als parameter (Branches)
		for(Branch b : Branches)
			für alle statements:
				rekursion mit branch und 
				
		Branches need to be saved or executed 
		
		Iterieren des AST iterativ oder rekursiv?
			rekursiv über den baum, return Branches, parameter List<String> for getting the conditions
			
			für jeden Branch in der Liste der Branches
				evaluate current statement and get newBranches
				recursion with newBranches
				Add new
Testen mit neuen Switch case (Pattern matching)
INhaltsverzeichnis
Einleitung
Related Work
Umsetzug 
Evaluierung
Conclusion
Bin bei EP1 Testkorrektur dabei
			
